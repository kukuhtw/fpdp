(() => {
  const key='fpdp_visitor_token',handle=document.body.dataset.profileHandle,status=document.querySelector('#cv-status');let cv=null;
  const api=async(path,options={},binary=false)=>{const headers={...(options.headers||{})},token=sessionStorage.getItem(key);if(token)headers.Authorization=`Bearer ${token}`;if(options.body)headers['Content-Type']='application/json';const response=await fetch(path,{...options,headers});if(binary&&response.ok)return response.blob();const payload=await response.json();if(!response.ok)throw new Error(payload?.error?.message||`Request failed (${response.status})`);return payload;};
  const message=(text,error=false)=>{status.textContent=text;status.className=`status ${error?'error':'success'}`;};
  const render=()=>{const host=document.querySelector('#cv-details');host.replaceChildren();const title=document.createElement('h2');title.textContent=cv.title;const price=document.createElement('p');price.className='cv-price';price.textContent=Number(cv.price_amount)>0?`${cv.price_currency} ${Number(cv.price_amount).toLocaleString('id-ID')}`:'Gratis';host.append(title,price);document.querySelector('#cv-actions').classList.remove('hidden');const signed=Boolean(sessionStorage.getItem(key));document.querySelector('#google-login').classList.toggle('hidden',signed);document.querySelector('#access-button').classList.toggle('hidden',!signed||cv.has_access);document.querySelector('#access-button').textContent=Number(cv.price_amount)>0?'Beli akses':'Aktifkan download';document.querySelector('#download-button').classList.toggle('hidden',!signed||!cv.has_access);};
  const load=async()=>{try{cv=(await api(`/api/v1/profiles/${encodeURIComponent(handle)}/cv`)).data;render();}catch(error){document.querySelector('#cv-details').innerHTML='<p class="muted">CV belum dipublikasikan.</p>';message(error.message,true);}};
  document.querySelector('#access-button').addEventListener('click',async()=>{try{const result=(await api(`/api/v1/profiles/${encodeURIComponent(handle)}/cv/access`,{method:'POST'})).data;if(result.granted){cv.has_access=true;render();return message('Akses diberikan. CV siap diunduh.');}if(result.payment?.payment_url){location.href=result.payment.payment_url;return;}message('Pembayaran diproses. Muat ulang setelah pembayaran dikonfirmasi.');}catch(error){message(error.message,true);}});
  document.querySelector('#download-button').addEventListener('click',async()=>{try{const blob=await api(`/api/v1/profiles/${encodeURIComponent(handle)}/cv/download`,{},true),url=URL.createObjectURL(blob),link=document.createElement('a');link.href=url;link.download=cv.title;link.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}catch(error){message(error.message,true);}});
  load();
})();
